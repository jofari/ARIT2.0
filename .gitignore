# Secrets
.env
# Overlay FreqUI : contient username/password/jwt_secret_key locaux (depot PUBLIC).
# Modele non secret : user_data/config.api.example.json
user_data/config.api.json

# Reglages Claude Code locaux (chemins machine, depot PUBLIC)
.claude/settings.local.json

# Runtime / donnees
backtest_lanes/
analysis/out/
user_data_run*/
user_data/macro_state.json
user_data/logs/
!user_data/logs/decisions/.gitkeep
user_data/state/
user_data/veto/
user_data/data/
user_data/backtest_results/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Pack de lancement (doublons — la spec canonique vit dans docs/)
ARIT_PDR_v3/
ARIT_claudecode_pack/
ARIT_PDR_v3_README.md
PROMPT_ARIT_BUILD.md

# Rapports de backtest generes (research/_reporting) : resultats de trading, depot PUBLIC.
# Le HTML se regenere en une commande, il n'a pas a etre versionne ni publie.
RAPPORT_TRADES*.html

# C1 (04/08) — calendrier economique : la source PRIMAIRE est VERSIONNEE (c'est tout son
# interet : elle marche sans reseau et survit a un clone). Seul le cache ForexFactory,
# qui est de la donnee fetchee et rafraichie chaque semaine, est ignore.
user_data/calendar/forexfactory_week.json
